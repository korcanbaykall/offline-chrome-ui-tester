[project]
name = "offline-chrome-ui-tester"
version = "0.1.0"
description = "Offline AI-driven UI tester (Playwright + lokal Ollama)"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40",
    "ollama>=0.6",
]

[dependency-groups]
dev = []
