[project]
name = "offline-chrome-ui-tester"
version = "0.1.0"
description = "Canlı Chrome'a CDP üzerinden bağlanıp UI testleri yapan offline sistem"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40",
    "pyyaml>=6.0",
    "rich>=13.0",
    "mlx>=0.30",
    "mlx-lm>=0.30",
    "ollama>=0.6",
    "requests>=2.31",
]

[tool.uv]
dev-dependencies = []
